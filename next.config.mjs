/** @type {import('next').NextConfig} */
const nextConfig = {
	// react-pdf 지원
	// webpack: (config) => {
	// 	config.resolve.alias.canvas = false;
	// 	return config;
	// },
};

export default nextConfig;
